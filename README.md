# AppInsight v4.0 — 通用文本评论情感分析可视化架构

<p align="center">
  <strong>一套可复用的多维度评论数据可视化分析框架</strong><br>
  以 AWARE App 评论数据集为示范案例，展示从原始评论文本到 <strong>20+ 种交互式图表</strong>的完整分析管线<br>
  <em>v4.0 专业升级：统计口径校准 · 正负独立色阶 · 多类型图表系统 · 4.0 数据探索体验</em>
</p>

<p align="center">
  <a href="https://www.haolin-zone.xyz">🌐 在线演示</a> ·
  <a href="#-快速启动">🚀 快速开始</a> ·
  <a href="#-技术栈">🛠 技术栈</a> ·
  <a href="#api-端点一览">📡 API</a> ·
  <a href="#-接入自定义数据集">🔧 接入指南</a>
</p>

---

## 🎯 核心价值：可视化架构，而非单一数据集

AppInsight 的本质不是「AWARE 数据集的可视化看板」，而是一套 **通用文本评论情感分析的可视化架构（Visualization Architecture）**。它的设计理念是：

> **只要你的数据包含「评论文本 + 情感标签 + 方面类别 + 评分」这几类字段，就能接入这套系统，自动生成同样丰富的多维度交互式分析界面。**

### 🔄 架构可扩展性

| 维度 | 说明 |
|:-----|:-----|
| **数据源无关** | 后端 `app.py` 的数据处理逻辑基于 Pandas DataFrame，只需替换数据文件和调整列名映射即可适配新数据集 |
| **领域可迁移** | 当前以 App 评论为案例，但同一架构可直接应用于：电商商品评价、酒店/餐厅点评、电影影评、社交媒体舆情、客服工单反馈等任意带标注的评论文本 |
| **方面类别自适应** | 玫瑰图、3D 气泡图、热力图等组件的类别轴动态读取数据中的唯一值，无需硬编码 |
| **图表组合即插即用** | 20 个图表组件独立封装，可根据新业务场景增删或重新组织页面布局 |
| **多数据集对比（v4.0）** | 原生支持按领域/来源分组的多数据集交叉对比视图，一键切换不同子集 |

<details>
<summary><b>📦 适用数据格式（7 个必填列名）</b></summary>

系统后端 `backend/app.py` 硬编码了以下列名，新数据集必须提供对应字段（CSV / JSON 均可）：

```
后端代码中的列名 ──→ 含义与要求
─────────────────────────────────────
sentiment        情感标签，值须为 'positive' 或 'negative'
category         方面类别（如：可用性、价格、性能…），字符串，用于玫瑰图/热力图/筛选器
rating           用户评分，数值型（通常 1-5），用于评分分析/排行榜/四象限/热力图
domain           领域分组（如：游戏、社交、工具…），字符串，用于雷达图/多数据集对比
app              被评价实体名称（如 App 名/商品名/店铺名），字符串，用于河流图/排行/四象限
sentence         评论文本原始内容，字符串，仅用于「评论长度分析」和「数据钻取」页面
term             分词后的关键词/术语，字符串列表或字符串，仅用于「词云图」和「关键词趋势」
```

> ⚠️ **关键点**：这 7 个列名是后端 Pandas 代码中直接引用的。接入新数据集时，**必须让数据的列名与之一致**。

</details>

---

## 🏗️ 系统架构总览

```
┌─────────────────────────────────────────────────────────┐
│                    AppInsight v4.0                        │
│                                                           │
│  ┌────────── Three.js 启动页 ─────────┐                   │
│  │   数据球体 · 沉浸式品牌入口            │  ← v4.0 升级     │
│  └────────────────────────────────────┘                   │
│                                                           │
│  ┌─────────── 全局筛选器 ───────────┐                     │
│  │  SENTIMENT: All / Pos / Neg     │  ← 跨页联动          │
│  │  CATEGORY:  All / 12类可选       │                     │
│  └─────────────────────────────────┘                     │
│                                                           │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐  六页导航    │
│  │总览│ │情感│ │方面│ │排行│ │对比│ │钻取│  (v4.0 扩展)   │
│  └──┬─┘ └──┬─┘ └──┬─┘ └──┬─┘ └──┬─┘ └──┬─┘             │
│     │      │      │      │      │      │                  │
│  ┌──▼──┐ ┌▼────┐ ┌▼────┐ ┌▼────┐ ┌▼───┐┌▼────┐        │
│  │仪表盘│ │玫瑰图│ │分布图│ │排行榜│ │四域│ │评论 │        │
│  │评分线│ │3D泡 │ │聚类卡│ │NPS值│ │交叉│ │钻取 │        │
│  │雷达图│ │词云 │ │      │ │详情│ │对比│ │搜索 │        │
│  │河流图│ │趋势 │ │      │ │    │ │    │ │排序 │        │
│  │     │ │热力图│ │      │ │    │ │    │ │导出 │        │
│  └─────┘ └─────┘ └─────┘ └─────┘ └───┘└─────┘        │
│                                                           │
│  ┌───────────────────────────────────┐                    │
│  │       Flask API 后端 (20+ 端点)   │  ← 数据聚合层      │
│  │   Pandas 清洗 → 分组统计 → JSON   │                    │
│  └───────────────────────────────────┘                    │
│                                                           │
│  ┌───────────────────────────────────┐                    │
│  │  GSAP 动画引擎 · ScrollTrigger     │  ← v4.0 优化      │
│  │  页面过渡 · 数字计数 · 滚动触发    │                    │
│  └───────────────────────────────────┘                    │
└─────────────────────────────────────────────────────────┘
```

### 分析管线

```
原始评论文本 → 情感标注 → 方面提取 → 评分关联
                                    ↓
              ┌─────────────────────┼─────────────────────┐
              ↓                     ↓                     ↓
        【总体层】            【评分层】             【方面层】
        正负比例              星级分布              类别热度
        领域对比              情感走势              正负拆解
                              ↓                     ↓
                        【行为层】             【实体层】
                        长度特征              App排行
                        热力密度              四象限定位
                              ↓                     ↓
                        【语义层(v4.0)】     【推荐层(v4.0)】
                        词云·关键词趋势       NPS净推荐值
                        主题聚类              多源交叉对比
```

---

## ✨ 功能特点

### 🖥 界面设计

- **六页导航架构（v4.0）**：总览 / 情感 / 方面 / 排行 / **多数据集对比** / **数据钻取**
- **全局筛选器**：按情感类型（正面 / 负面 / 全部）和方面类别（12 类可选）筛选，所有图表实时联动
- **卡片式布局**：每个图表模块独立成卡，配有标题、说明文字和关键数据摘要
- **Three.js 沉浸式启动页（v4.0）**：高质感数据球体与沉浸式品牌首屏
- **GSAP 动画系统（v4.0）**：页面平滑过渡、数字滚动计数、ScrollTrigger 滚动触发、悬停微交互

### 📈 可视化能力（20+ 种图表 × 6 大分析层次）

#### 第一层：总体概览（总览仪表盘）

| 图表 | 类型 | 回答的问题 | 可复用性 |
|:-----|:-----|:-----------|:---------|
| 情感仪表盘 | 仪表盘 + 环形图 | 整体正负面比例？ | ✅ 任何有情感标签的数据 |
| 评分情感分析 | 折线图 + 组合柱状图 | 各星级的正面/负面率？ | ✅ 任何有评分+情感的数据 |
| 领域雷达图 | 雷达图 | 不同领域各维度表现差异？ | ✅ 替换领域分类即可 |
| 热门 App 河流图 | 堆叠面积图 | Top 实体的情感趋势演变？ | ✅ 替换实体名+时间戳即可 |

#### 第二层：情感深度（情感深度分析）

| 图表 | 类型 | 回答的问题 | 可复用性 |
|:-----|:-----|:-----------|:---------|
| 方面玫瑰图 | 南丁格尔玫瑰图 | 各方面类别评论量分布？ | ✅ 动态读取类别 |
| 3D 气泡图 | 3D 气泡图（echarts-gl） | 方面 × 情感的立体对比？ | ✅ 动态读取类别 |
| 评论长度分析 | 分组柱状图 + 折线 | 评论长度与情感的关联？ | ✅ 任何文本数据通用 |
| 情感热力图 | 热力图 | 评分 × 方面的情感密度？ | ✅ 双轴动态 |
| 高频词云（v4.0 增强） | 词云图 + 关键词排名 | 用户最常提到的关键词？ | ✅ 任何分词语料 |
| 评分与情感关系（v4.0） | 正面率折线 + 区间 | 各星级的情感关系？ | ✅ 任何有评分+情感的数据 |
| 关键词搜索（v4.0） | 交互式搜索 | 快速定位含特定关键词的评论？ | ✅ 任何文本数据 |

#### 第三层：方面挖掘（方面挖掘与主题聚类）

| 图表 | 类型 | 回答的问题 | 可复用性 |
|:-----|:-----|:-----------|:---------|
| 方面类别分布 | 南丁格尔玫瑰图 | 用户最关注哪些方面？ | ✅ 动态读取类别 |
| 热门 App 情感对比 | 条形对比图 | 高评论量实体的各维度表现？ | ✅ 替换实体即可 |
| 主题聚类卡片（v4.0） | 聚类卡片组 | 评论自然聚合成哪些主题簇？ | ✅ 任何带标签数据 |

#### 第四层：实体排行（App 排行与竞争分析）

| 图表 | 类型 | 回答的问题 | 可复用性 |
|:-----|:-----|:-----------|:---------|
| 评分排行榜 | 水平条形图 | 哪些实体评分最高/最低？ | ✅ 任何有评分的数据 |
| 四象限分析 | 四象限散点图 | 评分 vs 讨论度的产品定位？ | ✅ 任何实体级数据 |
| NPS 净推荐值（v4.0） | NPS 刻度 + 分布 | 用户推荐意愿？Promoters/Detractors 拆分？ | ✅ 有评分+推荐字段的数据 |
| App 评分详情（v4.0） | 详情条形图 | 单个 App 的完整评分画像？ | ✅ 任何实体级数据 |

#### 第五层：多源对比（v4.0 · 多数据集对比）

| 图表 | 类型 | 回答的问题 | 可复用性 |
|:-----|:-----|:-----------|:---------|
| 四领域交叉对比 | 分组对比图表矩阵 | 综合/游戏/生产力/社交各维度差异？ | ✅ 替换领域分组即可 |
| 方面对比（v4.0） | 对比条形图 | 不同数据源的同一方面表现差异？ | ✅ 多源数据必备 |

#### 第六层：数据钻取（v4.0 · 数据浏览与钻取）

| 功能 | 类型 | 说明 | 可复用性 |
|:-----|:-----|:-----|:---------|
| 原始评论分页浏览 | 数据表格 | 逐条查看原始评论文本及标注信息 | ✅ 任何表格数据 |
| 关键词搜索 | 搜索过滤 | 在全量评论中搜索关键词并高亮 | ✅ 任何文本数据 |
| 多条件排序 | 排序切换 | 按评分/情感/方面/长度等多维排序 | ✅ 任何表格数据 |
| 数据导出 | 导出按钮 | 导出当前筛选结果（`/api/export`） | ✅ 任何聚合结果 |

### 🔧 交互特性

- **悬停提示**：每个数据点均支持 Tooltip 查看详情
- **平滑动画（v4.0 升级）**：GSAP 驱动的图表渲染过渡、数字滚动计数、页面切换动效
- **跨页联动**：筛选器变更后所有页面图表同步刷新
- **响应式布局**：适配桌面端浏览体验
- **滚动触发（v4.0 优化）**：ScrollTrigger 驱动的元素入场动画，增强叙事感

---

## 🛠️ 技术栈

### 前端

| 技术 | 用途 |
|:-----|:-----|
| Vue 3 + TypeScript | 核心框架 |
| Vite | 构建工具 |
| Vue Router 4 | 路由管理（v4.0，支持六页导航） |
| Pinia | 状态管理（v4.0，替代 Mitt 事件总线） |
| ECharts 5 / echarts-gl / echarts-wordcloud | 图表渲染引擎（20+ 图表全部基于此） |
| Three.js | 3D 渲染（v4.0，启动页数据球体） |
| GSAP 3 + ScrollTrigger | 动画引擎（v4.0，全局动画系统） |
| Element Plus | UI 组件库 |
| Axios | HTTP 请求 |

### 后端

| 技术 | 用途 |
|:-----|:-----|
| Flask | Web 框架 |
| Flask-CORS | 跨域支持 |
| Pandas + NumPy | 数据处理与分析（核心：`groupby` / `pivot_table` / `value_counts`） |
| Gunicorn | 生产环境 WSGI 服务器 |

### 部署

| 平台 | 用途 |
|:-----|:-----|
| **Vercel** | 前端静态托管（自动部署） |
| **Render** | 后端 API 托管（新加坡区域） |

---

## 🌐 在线演示

**网站地址**：[https://haolin-zone.xyz](https://haolin-zone.xyz)

> 当前线上版本使用 **AWARE 数据集（11,321 条 App 评论标注数据，含 4 个领域分区）** 作为示范案例，完整展示了整套可视化架构的分析能力。

---

## 🚀 快速启动

### 前置要求

- Node.js ≥ 20
- Python ≥ 3.11
- Git

### 使用默认 AWARE 数据

```bash
# 1. 克隆项目
git clone https://github.com/rnvale/myweb1.git
cd myweb1

# 2. 启动后端
cd backend
pip install -r requirements.txt
python app.py          # 或 python render_start.py（使用 uvicorn）
# → http://localhost:5000

# 3. 启动前端（新终端）
cd frontend
npm install
npm run dev
# → http://localhost:5173
```

打开浏览器访问 `http://localhost:5173` 即可。

<details>
<summary><b>🔧 接入自定义数据集</b></summary>

接入新数据集的核心工作是 **让数据的列名与后端代码一致**。有两种方式：

#### 方式一：在加载数据时 rename（推荐 ⭐）

只改 `backend/app.py` 文件头部的数据加载部分：

```python
# ====== 只需修改这一处 ======
df = pd.read_csv('your_data.csv')  # 替换为你的数据文件路径

df.rename(columns={
    '你的情感列名':      'sentiment',   # 值须为 'positive' / 'negative'
    '你的方面类别列名':   'category',    # 字符串，如 "可用性"/"价格"
    '你的评分列名':      'rating',       # 数值，通常 1-5
    '你的领域分组列名':   'domain',       # 字符串，如 "游戏"/"社交"
    '你的实体名称列名':   'app',          # 字符串，如 App 名/商品名
    '你的评论文本列名':   'sentence',     # 字符串，用于评论长度分析和数据钻取
    '你的关键词列名':     'term',         # 字符串，用于词云和关键词趋势（可留空）
}, inplace=True)
# ============================

# 其余 ~780 行 API 路由逻辑全部基于上面这 7 个标准列名工作，无需逐一改动
```

✅ **优点**：只需改文件开头几行，后续全部逻辑不用动。

#### 方式二：全局查找替换

```bash
sed -i "s/df\['app'\]/df['app_name']/g" backend/app.py
sed -i "s/\['sentiment'\]/['your_sent']/g" backend/app.py
# ... 以此类推，共 7 个列名
```

⚠️ **注意**：`app.py` 中这 7 个列名各被引用了 **10~60 次不等**（全文约 80+ 处引用），方式一更安全可控。

#### 前端需要改吗？

**一般不需要。** 前端图表组件从 API 获取的是已聚合后的 JSON 数据（数值 + 标签字符串），不直接接触原始列名。只要后端 API 返回的 JSON 结构不变，前端会自动适应：

- 类别筛选器 → 自动读取 `category` 的唯一值
- 图表标签 → 自动显示你的实体名和类别名
- 筛选联动 → 同样基于后端返回的值工作

唯一可能需要调整的地方：如果新数据集的情感标签值不是 `positive/negative`（例如是 `pos/neg` 或 `1/0`），则需要在前端筛选器按钮的文本上做对应修改。

</details>

### 构建生产版本

```bash
cd frontend && npm run build
# 构建产物输出到 frontend/dist 目录
```

---

## 📁 项目结构

<details>
<summary><b>点击展开项目目录结构</b></summary>

```
myweb1/
├── frontend/                       # Vue 3 + Vite 前端项目
│   ├── src/
│   │   ├── components/             # 20 个图表组件 + 筛选器组件（数据集无关）
│   │   │   ├── overview/           # 总览页：仪表盘、评分分析、雷达图、河流图
│   │   │   ├── sentiment/          # 情感页：玫瑰图、3D气泡、长度分析、热力图、词云、趋势
│   │   │   ├── aspect/             # 方面页：类别分布、App对比、主题聚类
│   │   │   ├── ranking/            # 排行页：评分排行、四象限、NPS、App详情
│   │   │   ├── compare/            # 对比页（v4.0）：四领域交叉对比、方面对比
│   │   │   └── drilldown/          # 钻取页（v4.0）：评论表格、搜索、排序、导出
│   │   ├── composables/            # GSAP 动画组合式函数（v4.0）
│   │   ├── stores/                 # Pinia 状态管理（v4.0）
│   │   ├── router/                 # Vue Router 配置（v4.0，六页路由）
│   │   ├── styles/                 # 主题变量（v4.0）
│   │   ├── utils/                  # 工具函数
│   │   ├── App.vue                 # 主布局（导航 + 路由 + 全局筛选器）
│   │   ├── http.ts                 # API 地址配置
│   │   └── main.ts                 # 入口文件
│   ├── vercel.json                 # Vercel 部署配置
│   └── index.html
├── backend/                        # Flask 后端项目
│   ├── app.py                      # API 主程序（~780 行，20+ 端点）
│   ├── data/                       # 数据集目录（AWARE，4 个领域分区）
│   ├── requirements.txt            # Python 依赖
│   └── render.yaml                 # Render 部署配置
├── wsgi.py                         # WSGI 入口（Render 使用）
├── render_start.py                 # 本地开发启动脚本（uvicorn）
├── runtime.txt                     # Python 版本指定（3.11）
└── README.md
```

</details>

---

## 📡 API 端点一览（20+）

<details>
<summary><b>点击展开完整 API 列表</b></summary>

```
概览类：
  GET /api/test              → 健康检查
  GET /api/meta              → 数据集元信息（各领域样本量、方面类别列表等）
  GET /api/summary           → 总体统计摘要

情感分析类：
  GET /api/aspect_sentiment  → 方面 × 情感分布（玫瑰图）
  GET /api/rating_sentiment  → 评分 × 情感分布（折线+柱状组合图）
  GET /api/sentiment_trend   → 评分与情感关系（v4.0）

领域与聚类类：
  GET /api/domain_compare    → 领域多维对比（雷达图）
  GET /api/aspect_stats      → 方面类别统计
  GET /api/topic_clusters    → 主题聚类结果（v4.0）

排行榜与竞争类：
  GET /api/top_apps          → Top App 排行榜
  GET /api/app_ratings       → App 评分详情（v4.0）
  GET /api/quadrant_scatter  → 四象限散点图数据
  GET /api/nps               → NPS 净推荐值计算（v4.0）

高级可视化类：
  GET /api/emotion_heatmap   → 情感热力图数据
  GET /api/length_analysis   → 评论长度分析
  GET /api/wordcloud         → 关键词词云数据（v4.0 增强）
  GET /api/keyword_trend     → 关键词趋势（v4.0）

多源对比类（v4.0）：
  GET /api/compare_datasets  → 多数据集综合对比
  GET /api/compare_aspects   → 方面级别跨数据集对比

数据钻取类（v4.0）：
  GET /api/drill_down        → 分页评论数据（支持筛选/搜索/排序参数）
  GET /api/export            → 导出当前筛选结果
```

</details>

---

## 📈 示范案例分析（AWARE 数据集）

> 以下结论来自当前线上演示所用的 **AWARE 数据集**，用于展示本套可视化架构所能产出的洞察深度。**换成你自己的数据集后，同样的图表将呈现属于你的洞察。**

基于 AWARE 数据集（**11,321 条**标注评论，12 个方面类别，4 个应用领域分区）：

1. **整体评价趋于平衡**：正面 50.1%，负面 49.9%
2. **三星用户是矛盾群体**：正面率仅 44.3%，最值得关注和挽回
3. **游戏 App 正面率最高**（52.4%），社交网络最低（44.7%）
4. **娱乐性是最大亮点**（正面率 69%），**可靠性是核心痛点**（负面率 73%）
5. **整体评价类最受关注**（约 3,000 条评论），远超其他方面类别
6. **长评论更倾向于负面表达**，用户在不满时更愿意详细阐述
7. **NPS 分析揭示**（v4.0）：通过评分分布推断用户推荐意愿，识别 Promoters 与 Detractors 群体
8. **跨领域对比洞察**（v4.0）：四个领域分区在相同方面的表现差异显著，指导针对性优化策略

---

## 📚 示范数据集

| 项目 | 说明 |
|:-----|:-----|
| **名称** | AWARE（App Review Analysis with Weak REcognition） |
| **地址** | [OpenDataLab/AWARE](https://opendatalab.com/OpenDataLab/AWARE) |
| **总规模** | **11,321 条**标注评论，12 个方面类别 |
| **领域分区** | Comprehensive（综合）、Games（游戏）、Productivity（生产力）、Social Networking（社交）|

> 💡 **说明**：AWARE 共 **11,321 条**标注评论。Games（~3,709）、Productivity（~3,774）、Social Networking（~3,118）为按领域划分的**子集/分区**，与 Comprehensive 存在重叠，并非独立叠加关系。v4.0 的「多数据集对比」页面支持按这些领域分区进行交叉分析。
>
> 💡 AWARE 仅为本项目的演示数据集。本系统的可视化架构可适配任意符合前述字段格式的文本评论数据。

---

## 🔧 云端部署说明

本项目采用 **前后端分离部署**方案：

### 前端（Vercel）

- 关联 GitHub 仓库，推送后自动构建部署
- `vercel.json` 配置构建命令和路由重写规则
- 构建完成后将 `frontend/dist` 静态托管

### 后端（Render）

- `render.yaml` 定义 Web Service 配置：

| 配置项 | 值 |
|:-------|:---|
| 运行时 | Python（Singapore 区域） |
| 构建 | `pip install -r backend/requirements.txt` |
| 启动 | `gunicorn backend.app:app --workers 1 --threads 2 --timeout 120` |
| 健康检查 | `/api/test` |

- `wsgi.py` 作为 WSGI 入口，`render_start.py` 用于本地 uvicorn 开发

### 本地 ↔ 云端切换

- 前端 `http.ts` 中修改 API base URL 即可切换后端地址（生产环境设置环境变量 `VITE_API_BASE_URL`）
- 后端 `app.py` 从环境变量读取端口，`host='0.0.0.0'` 适配云平台

---

## 📄 版本历史

### v4.0（当前版本）

- 🎨 **图表系统重构**：发散条形图、热力图、Dot Plot、四象限、NPS 刻度和关键词排名协同表达
- 📐 **统计口径校准**：情感筛选不再让比较类图表退化为 0% / 100%，并补充样本量与置信区间
- 🎯 **视觉规范升级**：浅色图表底、正负独立色阶、统一 Tooltip 和动态数据基线

- 🆕 **新增「多数据集对比」页面**：综合 / 游戏 / 生产力 / 社交四领域分区交叉对比
- 🆕 **新增「数据浏览与钻取」页面**：原始评论分页浏览、关键词搜索、多条件排序、数据导出
- 🆕 **新增 NPS 净推荐值评估**：Promoters / Passives / Detractors 三群拆分
- 🆕 **新增主题聚类卡片**：无监督主题发现可视化
- 🆕 **新增高频词云 & 关键词趋势**：语义层深度分析
- 🆕 **Three.js 沉浸式启动页**：粒子天体系统动画
- 🆕 **GSAP 全局动画系统**：页面过渡、数字计数、ScrollTrigger、悬停动效
- 🆕 **Vue Router + Pinia**：从侧边栏切换升级为正式路由架构
- 📊 **API 从 12 端点扩展至 20+**
- 🎨 **UI 全面升级**：主题变量系统、组件库迁移至 Element Plus

### v2.0

- 初始版本：4 大分析页面、12 种交互式图表、Mitt 事件总线联动

---

## 📄 开源协议

本项目仅供学习交流使用。

---

## 👤 作者

<p align="center">
  <a href="https://github.com/rnvale"><strong>Rain Vale</strong></a> ·
  <a href="https://haolin-zone.xyz">🌐 https://haolin-zone.xyz</a> ·
  <strong>v4.0</strong>
</p>
